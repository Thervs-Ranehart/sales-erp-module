# TODO: Sales by Product horizontal bar chart

- [ ] Update `ForecastingController@reports` to pass a UI-only `topProducts` dataset (5 items).
- [x] Replace the placeholder in `forecasting/reports.blade.php` with the reusable `components.top-products-horizontal-bar`.

- [x] Update `public/js/top-products-horizontal-bar.js` to sort **descending** (highest → least) to match the requirement.

- [ ] Ensure responsiveness: keep the original wrapper div sizing; set/adjust canvas container min-height if needed.

- [x] Manually verify by loading `/forecasting/reports`.


